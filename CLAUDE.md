# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

A minimal Python demo project with a single module (`greet.py`) that exposes a `greet(name, message="Hello")` function and a `__main__` entry point.

## Running the project

```bash
python greet.py
```

## Code structure

- `greet.py` — the entire codebase. Contains one function (`greet`) and a `__main__` block that calls it with `"World"`.
